# Runtime Data (`utl/pla/data`)

This directory stores local SQLite runtime files for the homelab playground.

- Default database path: `utl/pla/data/ply.db`
- Keep exports under `utl/pla/export/` for human-readable git diffs.
