Read and apply doc/pro/task/RULES.md first.

The active item filepath is given in the user prompt. Read it first.

Finalize this active item into completed state.

- Move file to `doc/pro/completed/yyyymmdd-hhmm_<topic>/` using the current close time
- Keep each file's existing filename timestamp prefix unchanged
- Ensure folder completion timestamp is >= every file timestamp prefix in that folder
- Set Status: completed
- Add final sections:
  - What changed (files, configs, behavior)
  - What was verified (tests/checks run, with commands and results)
  - What remains (follow-up items, if any; capture each as a new inbox item)
- If related review/result docs exist in active/, move them to the same
  topic folder
- Report: completed folder path + verification evidence summary
