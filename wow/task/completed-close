Read and apply wow/task/RULES.md first.

The active item filepath is given in the user prompt. Read it first.

Finalize this active item into completed state.

- This is the standard close path (bundle mode `off`).
- For grouped close destinations, use `wow/task/completed-close-bundle`.
- Resolve module key for the close leaf in this order:
  1. `module=<value>` in user prompt
  2. `Module: <value>` in optional `## Close Metadata`
  3. If still missing: stop and report `missing module key`; do not infer from
     filename/title and do not auto-fallback.
- `multi` is allowed only when explicitly provided (`module=multi` or
  `Module: multi`) for intentional cross-cutting closes.
- Reject workflow-state tokens as module keys:
  `inbox`, `queue`, `active`, `completed`, `dismissed`, `experiment`, `experiments`.
- Normalize module key: lowercase, replace spaces/underscores with `-`, remove non `[a-z0-9-]`, collapse repeated `-`, trim edge `-`.
- Move file to immutable leaf folder:
  `wow/completed/<current-close-timestamp>_<module>_<task-slug>/`
- Keep this v2 leaf format for all new standard closes.
- Keep each file's existing filename timestamp prefix unchanged
- Ensure leaf folder completion timestamp is >= every file timestamp prefix in that leaf
- Set Status: completed
- Add final sections:
  - What changed (files, configs, behavior)
  - What was verified (tests/checks run, with commands and results)
  - What remains (follow-up items, if any) with routing policy:
    - Default: capture each follow-up as a new inbox item
    - Exception: create directly in queue only when follow-up is mandatory,
      scope is clear, and priority is already locked
    - For direct queue routing, include in What remains:
      `Routing: queue (mandatory follow-up)` plus a one-line rationale
- Documentation closeout is mandatory:
  - In `What changed`, list documentation files updated (or state none).
  - In `What was verified`, include exactly one docs outcome token:
    - `Docs: updated` (with doc paths), or
    - `Docs: none` (one-line rationale), or
    - `Docs: deferred` (blocked reason + linked follow-up path)
  - `Docs: deferred` requires creating a follow-up item using the routing
    policy above (default inbox; queue only for mandatory, scope-clear,
    priority-locked follow-up).
  - If structural/public surfaces changed (new/renamed functions, signature
    changes, dependency changes, variable map changes), run
    `./utl/ref/run_all_doc.sh` and capture the result under `What was verified`.
- If related review/result docs exist in active/, move them to the same
  leaf folder
- Report: completed folder path + verification evidence summary
