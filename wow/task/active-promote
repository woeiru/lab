Read and apply wow/task/RULES.md first.

The active item filepath is given in the user prompt. Read it first.

Promote this active item into a program parent for parallel orchestration.

- Validate parent shape before promotion:
  - File is in `wow/active/`
  - Status is `active`
- Keep timestamp prefix stable (`yyyymmdd-hhmm_...`)
- If filename ends with `-plan.md`, rename to `-program-plan.md`
- If filename already ends with `-program-plan.md`, keep the current path
- Ensure parent includes required program sections:
  - `## Program Scope`
  - `## Global Invariants`
  - `## Workstreams`
  - `## Integration Cadence`
- Ensure parent includes `## Documentation Impact` with exactly one token:
  `Docs: required`, `Docs: none`, or `Docs: deferred`.
- In `## Workstreams`, add a scaffold for parallel planning with columns:
  - Workstream-ID, Scope, Depends-On, Touch-Set, Merge-Gate, Child-Plan, Status
- Preserve existing Goal/Context/Scope/Triage/Execution content unless needed
  for consistency with the new parent role
- Do not create child plans in this step (use `active-fanout` next)
- Report: original path + promoted path + sections added + fanout readiness
